#include <iostream>
using namespace std;


int main(){
    int a[100], size, search, mid, high, low=0;

    cout << "Enter size: ";
    cin >> size;
    high = size -1;
        for (int i =0; i<size; i++)
        {
            cout << "Enter a[" << i << "]: ";
            cin >> a[i];
        }

    cout << "Enter value to search: ";
    cin >> search;





    return 0;
}